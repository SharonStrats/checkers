import Game from "../../app/models/Game";

describe('Game', () => {
  test('it should create an empty game', () => {
    //let game = new Game(board, players,boardState, checkerState);
    console.log('it works');
  });
});